# decisions.md — save_anything 决策记录

> 规则:每次做出产品判断,Claude 主动问是否记录。
> 「为什么」两类:技术性判断由 Claude 直接写;个人偏好/真实动机写草稿并标 **(待我确认)**,你确认后去掉标记。
> 每次 commit 前检查有没有漏记的判断。

### 2026-09-11:理由输入方式定为交互式打字
**背景**:处理 inbox 文件时,需要用户写「为什么存它」的理由。用户最初设想中包含「或者我口述一句,你转文字」。
**选项**:A 交互式打字(ingest 逐条提问)/ B 语音转文字(接 ASR)/ C 预写在文件里(非交互)
**我选了**:A
**为什么**(待我确认):v1 保持零依赖、复杂度最低;口述场景可由 Claude 代跑 ingest 时转述输入,不需要真正的语音识别;ASR 引入重依赖与大量出错面,留到 v2。
**结果**:待填
**如果重来**:待填

### 2026-09-11:理由允许留空,标「(待补)」
**背景**:交互输入理由时是否强制必填。理由(为什么存)是产品核心,但强制会增加摩擦。
**选项**:A 可跳过,留空写「(待补)」/ B 必填,不写不入库(文件留 inbox)
**我选了**:A
**为什么**(待我确认):个人工具里强制只会让赶时间时卡住流程;用户本意就是要写,多数时候会写;留空有标记,之后可手动补。
**结果**:待填
**如果重来**:待填

### 2026-09-11:原文件处理完移到 archive/
**背景**:处理成功后 inbox 原文件的去向。
**选项**:A 移到 archive/(重名加时间戳)/ B 删除原文件 / C 留在 inbox 加扩展名标记
**我选了**:A
**为什么**(待我确认):可回溯、inbox 永远只装待处理文件、重跑不会重复处理;删除不可逆;留 inbox 会越来越乱。
**结果**:待填
**如果重来**:待填

### 2026-09-11:resurface 的回答记回笔记
**背景**:resurface 问「现在还有用吗?」之后,回答是否留存。
**选项**:A 记回笔记(正文末尾追加一行「- [时间] 回答」)/ B 只问不记
**我选了**:A
**为什么**(待我确认):将来做自动清理/淘汰需要历史回答数据;成本只是一行文本。
**结果**:待填
**如果重来**:待填

### 2026-09-11:resurface 选笔记规则=合并排序,「上次回顾」缺失用「保存时间」顶替
**背景**:最初设计「从未回顾组优先,其中保存时间最旧」。用户指出:今天刚存且未回顾的笔记会被优先推,但它刚存进来,不是最该回顾的;而最初重述的「缺失组优先」与旧规则等价,解决不了该问题。
**选项**:A 合并排序:每条取「最近看过时间」=上次回顾(缺失用保存时间顶替),推最早一条 / B 缺失组优先 / C 合并排序 + 新存 3 天宽限期
**我选了**:A
**为什么**(待我确认):一条规则全覆盖,无分组、无魔数;刚存的自然排最后,存得久又没看过的排最前。
**结果**:待填
**如果重来**:待填

### 2026-09-11:技术——零第三方依赖,纯标准库直调 DeepSeek
**背景**:技术栈约束「尽量少依赖,能用标准库就用标准库」。
**选项**:A 纯标准库(urllib 直调 HTTP)/ B requests / C openai SDK
**我选了**:A
**为什么**:本项目只有一次 HTTP POST 调用,urllib 完全够用;少依赖=少安装、少升级、少兼容问题;个人工具可维护性优先。
**结果**:待填
**如果重来**:待填

### 2026-09-11:技术——笔记头部用纯文本格式,不引 YAML 库
**背景**:笔记需要结构化头部(摘要/标签/理由/时间)+ 原文全文。
**选项**:A 手写 --- 分隔纯文本头部,行切分解析 / B YAML frontmatter + PyYAML
**我选了**:A
**为什么**:字段只有 6 个且值可能含冒号,行切分足够;PyYAML 是第三方依赖;用户可能手改笔记,YAML 缩进容错差。
**结果**:待填
**如果重来**:待填

### 2026-09-11:技术——「看过」=resurface 展示过,不用文件访问时间
**背景**:「最近没看过」需要定义「看过」。
**选项**:A resurface 展示过才算看过,记笔记头部「上次回顾」/ B 用文件 atime / mtime
**我选了**:A
**为什么**:Windows 下 atime 不可靠且常被禁用;mtime 无法区分「看过」与「编辑过」;A 可验证、可写回,语义与产品一致(被推送回顾过才叫回顾过)。
**结果**:待填
**如果重来**:待填

### 2026-09-11:技术——DeepSeek 输出三级兜底 + 重试细分
**背景**:LLM 输出不稳定(JSON 漂移、超字数、标签数不符;官方文档确认 JSON 模式有概率返回空 content)。
**选项**:A 严格校验,失败即跳过文件 / B 三级兜底(正则提取→字段规范化→原文前 30 字兜底)+ 重试只针对 429/5xx/网络错,401/402/400/422 不重试
**我选了**:B
**为什么**:ingest 单文件失败不应中断整批;兜底保证 notes 总有可用数据;对确定性错误重试纯属浪费请求。
**结果**:待填
**如果重来**:待填

### 2026-09-11:技术——编码策略 utf-8-sig→gb18030 兜底 + 控制台三流改 UTF-8
**背景**:Windows 中文环境,用户文件 UTF-8/GBK 混存;Git Bash(mintty)下 stdin 是管道,input() 收到的 UTF-8 字节会被按 GBK 解码成乱码。
**选项**:A 只按 UTF-8 读写 / B 读用 utf-8-sig→gb18030 兜底、写显式 UTF-8 + newline="\n"、stdin/stdout/stderr 全部 reconfigure 为 UTF-8
**我选了**:B
**为什么**:BOM 文件用普通 utf-8 读会残留 ﻿ 污染字段;gb18030 是 GBK 超集;stdin 不改,用户输入的中文直接乱码写进笔记。
**结果**:待填
**如果重来**:待填

### 2026-09-11:技术——model 固定 deepseek-chat 别名,不写具体版本名
**背景**:用户告知 DeepSeek 计划 2026-09-14 12:00 下线 V4 Pro,届时路由到 V4.1 Flash 并按后者计费。
**选项**:A model=deepseek-chat(平台自行路由版本)/ B 写具体版本名
**我选了**:A
**为什么**:别名随平台路由自动切换,外部版本变更对项目零影响;写死版本名反而会在平台变更后失效。
**结果**:待填
**如果重来**:待填

### 2026-09-11:技术——API 长度参数:前 6000 字截断 + max_tokens=300
**背景**:发给 LLM 的文本长度与输出 token 数需要设上限。
**选项**:A 全文直发、输出用默认 4K / B 只发前 6000 字(笔记正文存全文),max_tokens=300
**我选了**:B
**为什么**:摘要+标签输出很小,300 token 足够,显式设上限防止 JSON 被截断;6000 字远超「一句话摘要」所需信息量,且节省 token。
**结果**:待填
**如果重来**:待填

### 2026-09-11:桌面悬浮窗走纯 tkinter 重绘,悬停展开、移出延迟收起
**背景**:网页版「拾遗」完成后,想让它常驻屏幕右缘,鼠标一放上去就展开,随取随用。
**选项**:A 纯 tkinter 重绘面板(零依赖,美术细节打折)/ B tkinter 细条只负责唤起浏览器页面(简单但每次多一步)/ C WebView2 嵌入网页(视觉满分,但需 ctypes COM 互操作,出错率高且依赖 Edge 运行时)
**我选了**:A
**为什么**(待我确认):零第三方依赖是项目立身原则,不想为视觉引入 COM 级别的风险面;细条唤起不够「放上去就出来」。tkinter 是标准库自带,本机两个解释器都实测有 Tk 8.6,色块/圆角/衬线大字足以还原杂志风格。收起策略选「移出延迟 3 秒 + 钉住按钮」,是怕打字时鼠标滑出面板误收起。
**结果**:待填
**如果重来**:待填
